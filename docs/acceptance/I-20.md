# I-20 Acceptance — Onboarding-first Forge Shell + Account-level Assumption Chips

## 结论

- Status: Done
- Date: 2026-06-24
- Scope implemented: onboarding-first `/forge` shell, optional prior-post cold start, three account-level direction assumptions, rationale/confidence display, edit flow, draft autosave, API/type/prompt anchors for `accountPost`, `rationale`, `confidence`.
- OpenRouter `401` blocker resolved after Owner rotated/restored ForgeNote runtime `OPENROUTER_API_KEY`.
- Successful generation verified with `OPENROUTER_MODEL=openai/gpt-4o-mini`.

## 自动验证

```bash
npm run lint
npm run typecheck
npm run build
```

Result: pass.

Notes:
- First `npm run build` inside sandbox failed because `next/font` could not fetch Geist from Google Fonts.
- Re-running with network permission passed.

## Chrome 验收路径

Environment:
- Local dev server: `http://localhost:3000`
- Browser: real Chrome logged in as `huggingme@duck.com`
- Route: `/forge`

Steps and evidence:
1. Opened `/forge`.
   - Result: logged-in protected route rendered directly.
   - First screen shows app workbench, not marketing header.
   - Visible primary headline: `把一个模糊想法，变成可以直接开始做的内容方案`.
2. Pasted input: `想做一组第一次独居备用金清单的图文卡片`.
   - Result: primary action enabled.
3. Clicked `生成内容方案`.
   - Result: did not call model immediately; entered direction review state.
   - Visible direction title: `我先帮你定了这次的方向，改不对的就行`.
   - Visible assumptions: `受众` / `内容形式` / `表达角度`.
   - Each assumption showed confidence and `依据` action.
4. Edited `受众`.
   - Selected `新手父母`.
   - Result: chip changed to `新手父母`, confidence became `确定`, confirmed count became `已确认 1/3`.
5. Clicked final `生成内容方案`.
   - First run before key restoration: loading state rendered, then error state rendered cleanly.
   - Error message: `OpenRouter 返回非 2xx 状态：401`.
6. Owner restored ForgeNote runtime OpenRouter key and kept `OPENROUTER_MODEL=openai/gpt-4o-mini`.
   - `npm run doctor` confirmed `OPENROUTER_API_KEY` and `OPENROUTER_MODEL` are configured without printing values.
7. Re-ran final `生成内容方案`.
   - Result: `POST /api/forge 200`.
   - Generated session: `63ec12d9-2f8c-4b76-9ed1-6474b837e5a4`.
   - Result UI rendered outcome, copy actions, recipe panel, verification block.
8. Fixed and verified two follow-up issues found during retest.
   - Draft autosave restore no longer causes the button/hydration mismatch after a clean dev-server restart.
   - Recipe panel maps internal `content_package` to user-facing copy instead of rendering the raw key.

## 残余风险

- Chrome session URL re-open check was not repeated after the final copy mapping fix because local GUI-open approval was rejected by the host environment. Code path is covered by `RecipePanel` mapping + lint/typecheck/build.
- Open Design BYOK and ForgeNote runtime env remain separate systems; future key rotations must update ForgeNote `.env.local` / deployment env explicitly.

## Files Covered

- `src/components/forge/ForgeWorkbench.tsx`
- `src/components/forge/IdeaInput.tsx`
- `src/components/forge/DirectionPanel.tsx`
- `src/components/forge/RecipePanel.tsx`
- `src/app/api/forge/route.ts`
- `src/lib/ai/types.ts`
- `src/lib/ai/generate.ts`
- `src/lib/ai/mock-generator.ts`
- `src/lib/copy/zh-Hans.ts`
- `src/lib/copy/en.ts`
- `src/app/forge/page.tsx`
