# I-19 Acceptance — Production 上线就绪 + 首批真实用户路径验收 + DB 指标读出

> 票：把 M1 推到真实用户可登录使用的 Production，按 OPERATING-MODEL Gate 3/4 拿到第一份真实使用证据；产出只读指标脚本，从现有库表算出 6 个验证指标（无第三方 SDK）。
> **状态：Done（Owner 接受 Conditional Pass，2026-06-23）。** Gate 2 通过；Production 配置完成并冒烟通过；生产 OAuth 登录往返实测通过；Gate 4 指标已从生产库读出（测试账号样本）。生成→保存→重跑→表现段为 Preview 同码、Preview 登录态已实测通过，Owner 据此接受 Conditional Pass 收口。残余风险：Production 上尚无外部真实用户内容路径证据（已记录在案）。
> Production URL：`https://forge-note-gold.vercel.app`

## 范围内（Claude Code，已交付）

- `scripts/metrics.mjs`：只读聚合，输出 6 个指标；无 `DATABASE_URL` → SKIP exit 0；只取整数计数，不取输入全文/生成正文；不打印 secret、不内嵌 key、不写库。
- `package.json` 增 `metrics` script；`scripts/doctor.mjs` 增 `scripts/metrics.mjs` 存在性 + `metrics` script 检查。
- `docs/DEPLOYMENT.md`：Production 上线就绪清单（env 分区 / Supabase OAuth 含 Client Secret / migration 0001→0002 / Deployment Protection / 自动验证 / Gate 3 / 指标读出）。
- `docs/RUNBOOK.md`：metrics 运行方式与判定语义、安全约束。

## 范围外

- 不接 PostHog / Sentry SDK、不改 `observability.ts` 行为；不做 runtime i18n / locale 切换。
- 不加任何产品功能、不改 schema / migration / RLS / prompt / API / 既有路由。

## 自动验证（Gate 2）

| 命令 | 结果 |
|---|---|
| `npm run doctor` | 通过（0 failed / 0 warnings；识别 `metrics` script + `scripts/metrics.mjs`） |
| `npm run lint` | 通过（exit 0） |
| `npm run typecheck` | 通过（exit 0） |
| `npm run build` | 通过（exit 0；路由表不变，未新增运行时路由） |
| `npm run metrics`（无 DATABASE_URL） | 通过（SKIP exit 0，不报错、不打印 secret） |

> 注：`npm run metrics`（有库读出）与 `npm run db:test-rls` 需连 Production DB，属 Gate 4 / Owner 配置后执行。

## Codex QA Agent 复核（Gate 2，实证）

> 不只读代码：在本地 PostgreSQL 16 建一次性隔离库，应用 metrics 读到的 `sessions`/`recipes` 列子集，灌入确定性合成数据，跑**真实** `npm run metrics`，比对手算值，跑完即删库（既有库未碰）。

合成数据（3 用户）：u1 两个 completed（其一含 `edited` 假设、其二为 rerun-origin 且回填表现，跨 2 自然日）；u2 一个 completed（无 edited、无表现）；u3 仅 draft。

| 指标 | 手算 | 脚本读出 | 一致 |
|---|---|---|---|
| activation_rate | 2/3 | 2/3 (66.7%) | ✅ |
| assumption_edit_rate | 1/3 | 1/3 (33.3%) | ✅ |
| recipe_save_rate | 2/3 | 2/3 (66.7%) | ✅ |
| recipe_rerun_rate | 1/1 | 1/1 (100.0%) | ✅ |
| return_session_rate | 1/3 | 1/3 (33.3%) | ✅ |
| performance_fill_rate | 1/3 | 1/3 (33.3%) | ✅ |

- **只读证明**：跑前/后行数完全一致（`sessions=4` / `recipes=2`）；源码零 `INSERT/UPDATE/DELETE`；临时库 `drop database` 收尾。
- **不泄露证明**：跨进程仅整数；`raw_input`/`outcome`/`recipe_snapshot`/`performance_note` 仅出现在注释与 `IS NOT NULL` 判断，从不 SELECT 其值。
- **无新依赖**：psql 子进程，与 `db:test-rls` 同款。
- **QA 语义提示（非阻断）**：`recipe_save_rate` 计入已删配方（「曾保存过」），`recipe_rerun_rate` 仅算未删配方（库内可重跑性）——两者回答不同问题、对删除的处理刻意不同，建议 Owner 知悉认可。

**QA Agent 结论：Conditional Pass。** Gate 2 实现正确性以实证通过；Gate 3（Production 真实用户路径）/ Gate 4（指标读出）待 Owner 完成 Production 配置后补证据（已记为残余风险）。

## Production 配置实测（OPS-02，本会话浏览器实操）

> 在 Owner 逐项授权下，通过浏览器对 Vercel / Supabase 控制台完成 Production 上线配置，并冒烟验证生产站可达。项目：Vercel `forge-note`（域名 `forge-note-gold.vercel.app`）、Supabase `tsqgetxhyitltgztxymd`。

| 配置项 | 操作 | 结果 |
|---|---|---|
| Vercel env（4 必需） | `NEXT_PUBLIC_SUPABASE_URL`/`_ANON_KEY`/`OPENROUTER_MODEL`/`OPENROUTER_API_KEY` 原仅 Preview，改为 **Production + Preview** | ✅ 四项均显示 “Production and Preview” |
| Vercel 部署 | 保存 env 触发新部署 | ✅ “Deployment created” |
| Vercel Deployment Protection | Vercel Authentication “Require Log In” 由 ON → **OFF** 并保存 | ✅ 公网可达（不再 401 SSO） |
| Supabase Redirect URLs | 新增 `https://forge-note-gold.vercel.app/auth/callback` | ✅ Total URLs: 3（含 localhost / 旧 Preview 别名 / Production） |
| Supabase Google provider | Enabled；Client ID 已填；**Client Secret 已填**（Owner 确认） | ✅ 齐全（呼应 Preview 期 blocker 根因已消除） |
| 生产 `/login` 冒烟 | 打开 `https://forge-note-gold.vercel.app/login` | ✅ 正确渲染产品名 + Google 登录 + 邮箱链接；无 401 / 「未配置」/ 白屏 |

- 安全：全程未读取/打印任何 secret 值；env 仅多挂一个环境目标，未改值。`OPENROUTER_API_KEY` 值未读。
- 残余：Deployment Protection 已对公网开放——用户数据由应用自身 Supabase 登录 + RLS 保护，未认证访问 `/api/forge` 仍 `AUTH_REQUIRED`。

## Gate 3 — 真实用户路径验收（基础设施已就绪，待真实用户主路径实测）

- **验收环境**：Production（URL：`https://forge-note-gold.vercel.app`）
- **用户身份**：真实新用户 / 已登录用户（______）
- **操作路径与实测**：

| 步骤 | 预期 | 实测 |
|---|---|---|
| 登录（Google OAuth） | 成功进入 `/forge` | ✅ **生产已实测**：点「使用 Google 登录」→ `/auth/callback` → `/forge`，TopNav 显示 `dennisliu1225@gmail.com`，含 Forge/配方库/偏好/退出。新 redirect URL 生效，Preview 期 Client Secret blocker 已消除 |
| 输入模糊想法 | 可输入、不白屏 | 待真实用户（同 Preview 已过代码） |
| 看懂假设条 / 改一项默认假设 | 假设条可读、可编辑 | 待真实用户 |
| 成功生成内容包 | Outcome/Recipe/Verification 完整 | 待真实用户 |
| 保存配方 | 「已保存到配方库」 | 待真实用户 |
| 刷新页面后找回结果 | `/forge?session=` 或 `/recipes` 仍在 | 待真实用户 |
| 配方详情换输入重跑 | 落新 session，原 recipe 未覆盖，`usage_count`+1 | 待真实用户 |
| 记录发布表现 | 「已记录表现」 | 待真实用户 |
| 回看时表现仍在 | 重开预填回原值 | 待真实用户 |

- **已实测（生产特有风险）**：登录 OAuth 往返 + 受保护路由 `/forge` 未登录正确重定向 `/login` + 登录后渲染完整。这是 Production 相对 Preview 的唯一新风险面，已通过。
- **方法学边界**：生成→保存→重跑→表现回填→回看属用户内容路径，**应由真实用户产生证据**——代理模拟会伪造「真实用户」结论，违背 OPERATING-MODEL Gate 3，故不由代理代跑（亦避免无意义的计费生成与写库）。该段为**与 Preview 完全相同的代码**，已在 Preview 真实 Chrome 登录态实测通过（见 `docs/PROJECT-STATUS.md`：session `939ec634-…` 生成/回看、I-12 表现回填等）。
- **残余风险**：Production 上该内容路径尚无**外部真实用户**证据；建议 Owner（或首批真实用户）走一遍并补本表，或显式接受「Preview 同码已验」作为 Conditional Pass 依据。
- **证据**：Production URL `https://forge-note-gold.vercel.app`；登录态 TopNav 截图；Gate 4 指标读出见下。

## Gate 4 — 指标读出（待 Production 有数据后执行）

```bash
DATABASE_URL='postgres://...' npm run metrics
```

读出方式：直连 Production DB 被 Owner 本机代理（fake-ip，仅隧道 HTTP/HTTPS）挡掉裸 5432 TCP；改用 Supabase **SQL Editor**（HTTPS 穿代理）运行 metrics 同口径只读查询。**首批读出（2026-06-23）**：

| 指标 | 命中/分母 | 比率 |
|---|---|---|
| activation_rate | 2/2 | 100.0% |
| assumption_edit_rate | 2/12 | 16.7% |
| recipe_save_rate | 1/12 | 8.3% |
| recipe_rerun_rate | 1/1 | 100.0% |
| return_session_rate | 1/2 | 50.0% |
| performance_fill_rate | 2/12 | 16.7% |

分母：用户数=2、completed session=12、在库配方（未删）=1。

> **样本性质（残余风险）**：当前数据来自 M1 开发期的测试账号（2 个用户、12 次生成），**非外部真实用户**，故比率仅证明「指标可从生产库读出」，暂无产品判断意义。小样本噪声大：本票只要求「能读出」，不设达标阈值。I-19 Done 后由 Codex/Owner 再定是否进入观测 SDK 接入 / runtime i18n（仍需**真实用户**指标证据支撑）。

## 结论

- **Gate 2（实现正确性）**：Pass（自动验证 + 本地一次性库实证只读）。
- **Production 配置 + Gate 3 基础设施**：Pass —— 4 env 挂 Production、Deployment Protection 关、Supabase redirect/Google 齐全、`/login` 渲染、`/forge` 鉴权重定向、**Google OAuth 登录往返生产实测通过**。
- **Gate 4（指标读出）**：Pass —— 经 SQL Editor 从生产库读出 6 指标（2026-06-23）；样本为测试账号，仅证「可读出」。
- **Gate 3（用户内容路径）**：**Conditional Pass（Owner 已接受，2026-06-23）** —— 生产 OAuth/鉴权已实测；生成→保存→重跑→表现段为 Preview 同码、已在 Preview 登录态实测通过。不由代理模拟以免伪造结论。Owner 据「Preview 同码已验 + 生产基础设施/OAuth 已实测」接受 Conditional Pass。
- **I-19 状态：Done。** 后续由 Codex/Owner 定下一票（仍需**真实用户**指标证据，不默认 SDK / runtime i18n）。当 Production 出现外部真实用户后，应回填本文 Gate 3 表并复核 Gate 4 指标。
