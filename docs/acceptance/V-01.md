# V-01 Acceptance — Small Real-User Validation

## 结论

- Status: Ready
- Date opened: 2026-06-25
- Goal: observe 1-3 non-builder users on Production and record whether they can complete ForgeNote's M1 loop without Codex or Owner driving the product decisions.
- Environment target: Production `https://forge-note-gold.vercel.app`
- Scope: validation only; no product code, prompt, schema, RLS, visual rendering, asset library, Stripe, runtime i18n, or automatic learning.

## Gate 1 — Product Intent

- User: a non-builder creator or creator-adjacent user with a real carousel / graphic post idea.
- Task: log in, enter a fuzzy idea, understand or edit one assumption, generate a content plan, decide whether to save it as a recipe, open the recipe detail, and rerun it with a new input.
- Changed path being tested: the full M1 loop that now exists after I-20, I-22, and I-23.
- Loss if not done: ForgeNote keeps accumulating features without knowing whether a real user can understand the loop or sees enough value to save and rerun.
- Out of scope: feature requests during the session; record them, do not implement them in V-01.

## Test Script

Read only this short framing to the user:

```text
请用你自己的一个真实内容想法试 ForgeNote。
目标不是评价你写得好不好，而是看这个工具能不能帮你从模糊想法走到可复用的内容方案。
过程中请尽量自己操作；卡住时直接说你卡在哪里。
```

Do not explain hidden product concepts such as schema, recipe internals, metrics, or tickets.

## Observer Rules

- Do not choose the topic for the user.
- Do not tell the user which assumption to edit.
- Do not tell the user whether the generated content is "good enough".
- Help only with account/login infrastructure if blocked.
- Record exact blockers and visible page states.

## Validation Execution Readiness — 2026-06-28 (Claude Code)

> 角色边界：Claude Code 只做技术就绪复核与如实记录，不冒充真实用户。下面的 Per-User Record 必须由 Owner 与一个真实非构建者用户在 Production 跑出来后才能填，未跑前一律留空。

技术前置条件复核（范围内第 1 项「Production 主路径是否具备真实用户测试条件」）：

```text
- npm run doctor                : PASS（0 failed / 0 warnings；env 4 必需项 + OpenRouter 已配置）
- Production /login             : HTTP 200（登录页可渲染）
- Production /forge（未登录）   : HTTP 307 → /login（鉴权闸正常）
- FORGENOTE_BASE_URL=https://forge-note-gold.vercel.app npm run smoke:api : PASS
    非法 JSON → 400 VALIDATION_FAILED
    缺 rawInput → 400 VALIDATION_FAILED
    合法输入未登录 → 401 AUTH_REQUIRED
    空白输入仍先被鉴权拦截 → 401 AUTH_REQUIRED
- 代码基线: main HEAD b42a33b（c62065f / I-23 之后的文档提交）
```

结论：Production 平台层（可达性 / 登录页 / 鉴权闸 / 匿名 API 边界）就绪，可以承接真实用户测试。**但平台就绪 ≠ V-01 通过**——验收标准要求至少 1 个真实非构建者用户完成或明确卡住，目前为零真实用户证据。

仍缺、且不在 Claude Code 可独立完成范围内的两项（决策点交 Owner / Codex）：

1. **真实非构建者用户**：登录态生成 / 保存 / 重跑路径需真人操作并由观察者记录。Claude Code 不能驱动登录态、也不得冒充真实用户（OPERATING-MODEL：不把自测冒充真实用户验证）。需 Owner 约到至少 1 名真实用户共享屏幕或现场操作。
2. **指标读出**：本机无法读 Production 指标（见 Gate 4 区块的阻塞记录）；需 Owner 在 Supabase SQL Editor 执行已备好的 SQL。

## Owner Dry Run Blocker — 2026-06-28

Owner 在 Production 真实使用前发现 Forge 入口存在主路径阻塞：

```text
- 输入内容后，用户以为点击「生成内容方案」就会生成，但实际只进入方向确认；第二个真正生成按钮在下方，路径不清楚。
- 空态顶部写死「正在写这次的想法」，但用户还没开始写，状态文案错误。
- 「输出语言 / 表达偏好」藏在折叠区里，没有快捷选项；用户感知为没有可选、输入没反应。
```

Codex 判定：

```text
- 这不是新功能需求，而是 V-01 前必须修的入口交互阻塞。
- 修复边界：只改 /forge 前端交互文案与控件可见性；不改 API、prompt、DB、RLS、配方、资产库、视觉渲染。
- V-01 仍不能标 Done；修复合入后重新让真实用户跑 Production 主路径。
```

### V-01-FIX-01 implementation — 2026-06-28

Implemented minimal blocker fix:

```text
- Idle shell status changed from “正在写这次的想法” to “输入一个想法开始”.
- First-step CTA changed from “生成内容方案” to “先确认方向”.
- After first-step CTA, the direction confirmation panel scrolls into view so the real generation step is visible.
- “输出语言 / 表达偏好” is now visible by default, with quick choices:
  中文 / English / Instagram carousel / LinkedIn carousel / 清空.
```

Validation:

```bash
npm run doctor
npm run lint
npm run typecheck
npm run build
FORGENOTE_BASE_URL=http://127.0.0.1:3000 npm run smoke:api
```

Result:

```text
- doctor: PASS（0 failed / 0 warnings）
- lint: PASS
- typecheck: PASS
- build: PASS after rerun with network permission for next/font Google Fonts
- smoke:api: PASS after rerun in the same permission domain as the local dev server
```

Preview Gate 3:

```text
- PR: #15
- Preview: https://forge-note-git-codex-v-01-forge-entry-fix-lstosts-projects.vercel.app
- User: dennisliu1225@gmail.com
- OAuth: Google login returned to PR #15 Preview /forge.
- Idle shell showed: 输入一个想法开始.
- Output language / expression preference was visible by default.
- Quick choice “中文” filled the field with `zh-Hans`.
- Test input: 想做一组第一次养猫预算清单的图文卡片
- First CTA: 先确认方向
- After first CTA: direction confirmation panel appeared with the second-step CTA `生成内容方案`.
- Second-step generation entered loading state and completed successfully.
- Result URL: /forge?session=c9a89de2-149a-401f-b6c8-cc689f9e7ae7
- Result included: 发布正文, 5 页逐页卡片文案, 配图方向, 发布前检查, 全部通过, and locale meta `zh-Hans`.
- Browser console: no error/warning logs captured during final result check.
```

V-01-FIX-01 result: Pass on Preview. V-01 itself is still not Done; after merge, run the real non-builder Production validation.

## Per-User Record

Duplicate this block for each user.

```text
User:
- Date:
- Environment:
- User type:
- Login method:
- Topic entered:

Path:
- Login completed: pass / fail / blocked
- First generation completed: pass / fail / blocked
- Assumptions understood: yes / no / unclear
- Assumption edited before generation: yes / no
- Output judged useful enough to continue: yes / no / unclear
- Recipe saved: yes / no
- Recipe detail opened: yes / no
- Recipe rerun completed: yes / no

Evidence:
- Source session id:
- Recipe id:
- Rerun session id:
- Visible blocker or confusion:
- User quote or paraphrased reaction:

Observer conclusion:
- Continue validating / fix blocker / open implementation ticket:
```

## Gate 4 — Metrics Readout

After each session batch, run:

```bash
npm run doctor
npm run metrics
```

Record the metric snapshot here:

```text
Metric readout date:
- activation:
- assumption_edit:
- recipe_save:
- recipe_rerun:
- return_session:
- performance_fill:
- Command result:
- Blocker if metrics unavailable:
```

### Metrics readout attempt — 2026-06-28 (Claude Code)

```text
Metric readout date: 2026-06-28 (not yet read)
- activation: —
- assumption_edit: —
- recipe_save: —
- recipe_rerun: —
- return_session: —
- performance_fill: —
- Command result: `npm run metrics` → SKIP exit 0（本地未设 DATABASE_URL / SUPABASE_DB_URL）。
- Blocker if metrics unavailable:
    本机直连 Production Postgres 不可行——
    (1) 本机代理 fake-ip 挡掉裸 5432（与 I-19 同一阻塞，见 docs/acceptance/I-19.md）；
    (2) 用 .env.local 的 SUPABASE_DB_PASSWORD 构造直连串被 Claude Code guardrail 拦截（[Production Reads]，未获授权）。
    既定读出路径：Owner 在 Supabase SQL Editor 手动执行下方 SQL（与 scripts/metrics.mjs 等价，只读聚合，不取任何输入全文/正文）。
```

供 Owner 在 Supabase SQL Editor 直接粘贴执行（只读，仅返回整数计数与比率）：

```sql
with m as (
  select
    (select count(distinct user_id) from public.sessions) as users_total,
    (select count(distinct user_id) from public.sessions where status='completed') as users_activated,
    (select count(*) from public.sessions where status='completed') as sessions_completed,
    (select count(*) from public.sessions s
       where s.status='completed'
         and exists (select 1 from jsonb_array_elements(s.assumptions) e where e->>'state'='edited')) as sessions_edited,
    (select count(*) from public.sessions s
       where s.status='completed'
         and exists (select 1 from public.recipes r where r.source_session_id=s.id)) as sessions_with_recipe,
    (select count(*) from public.recipes where deleted_at is null) as recipes_total,
    (select count(*) from public.recipes where deleted_at is null and usage_count>0) as recipes_rerun,
    (select count(*) from (
       select user_id from public.sessions group by user_id
       having count(distinct (created_at at time zone 'utc')::date) > 1) t) as users_returned,
    (select count(*) from public.sessions s
       where s.status='completed'
         and (s.published_at is not null or s.like_range is not null or s.favorite_range is not null
              or s.comment_range is not null or s.follower_gain_range is not null
              or s.performance_note is not null)) as sessions_perf_filled
)
select
  users_activated || '/' || users_total       as activation_rate,
  sessions_edited || '/' || sessions_completed as assumption_edit_rate,
  sessions_with_recipe || '/' || sessions_completed as recipe_save_rate,
  recipes_rerun || '/' || recipes_total        as recipe_rerun_rate,
  users_returned || '/' || users_total         as return_session_rate,
  sessions_perf_filled || '/' || sessions_completed as performance_fill_rate
from m;
```

## Completion Criteria

- At least 1 non-builder user completes or clearly fails at a named step in the Production path.
- The record includes environment, user type, topic, step outcomes, session/recipe ids when available, and the observer conclusion.
- Metrics are read out or the exact metrics blocker is recorded.
- Final status is one of:
  - Pass: at least one user completes first generation, save, and rerun without product-critical confusion.
  - Conditional Pass: the core path works but a concrete friction point must become the next ticket.
  - Fail / Blocked: login, generation, save, rerun, or metrics cannot be completed.
