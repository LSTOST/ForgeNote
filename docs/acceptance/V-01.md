# V-01 Acceptance — Small Real-User Validation

## 结论

- Status: Ready
- Date opened: 2026-06-25
- Goal: observe 1-3 non-builder users on Production and record whether they can complete ForgeNote's M1 loop without Codex or Owner driving the product decisions.
- Environment target: Production `https://forge-note-gold.vercel.app`
- Scope: validation only; no product code, prompt, schema, RLS, visual rendering, asset library, Stripe, runtime i18n, or automatic learning.

## Gate 1 — Product Intent

- User: a non-builder creator or creator-adjacent user with a real carousel / graphic post idea.
- Task: log in, enter a fuzzy idea, understand or edit one assumption, generate a content plan, decide whether to save it as a recipe, open the recipe detail, and rerun it with a new input.
- Changed path being tested: the full M1 loop that now exists after I-20, I-22, and I-23.
- Loss if not done: ForgeNote keeps accumulating features without knowing whether a real user can understand the loop or sees enough value to save and rerun.
- Out of scope: feature requests during the session; record them, do not implement them in V-01.

## Test Script

Read only this short framing to the user:

```text
请用你自己的一个真实内容想法试 ForgeNote。
目标不是评价你写得好不好，而是看这个工具能不能帮你从模糊想法走到可复用的内容方案。
过程中请尽量自己操作；卡住时直接说你卡在哪里。
```

Do not explain hidden product concepts such as schema, recipe internals, metrics, or tickets.

## Observer Rules

- Do not choose the topic for the user.
- Do not tell the user which assumption to edit.
- Do not tell the user whether the generated content is "good enough".
- Help only with account/login infrastructure if blocked.
- Record exact blockers and visible page states.

## Validation Execution Readiness — 2026-06-28 (Claude Code)

> 角色边界：Claude Code 只做技术就绪复核与如实记录，不冒充真实用户。下面的 Per-User Record 必须由 Owner 与一个真实非构建者用户在 Production 跑出来后才能填，未跑前一律留空。

技术前置条件复核（范围内第 1 项「Production 主路径是否具备真实用户测试条件」）：

```text
- npm run doctor                : PASS（0 failed / 0 warnings；env 4 必需项 + OpenRouter 已配置）
- Production /login             : HTTP 200（登录页可渲染）
- Production /forge（未登录）   : HTTP 307 → /login（鉴权闸正常）
- FORGENOTE_BASE_URL=https://forge-note-gold.vercel.app npm run smoke:api : PASS
    非法 JSON → 400 VALIDATION_FAILED
    缺 rawInput → 400 VALIDATION_FAILED
    合法输入未登录 → 401 AUTH_REQUIRED
    空白输入仍先被鉴权拦截 → 401 AUTH_REQUIRED
- 代码基线: main HEAD b42a33b（c62065f / I-23 之后的文档提交）
```

结论：Production 平台层（可达性 / 登录页 / 鉴权闸 / 匿名 API 边界）就绪，可以承接真实用户测试。**但平台就绪 ≠ V-01 通过**——验收标准要求至少 1 个真实非构建者用户完成或明确卡住，目前为零真实用户证据。

仍缺、且不在 Claude Code 可独立完成范围内的两项（决策点交 Owner / Codex）：

1. **真实非构建者用户**：登录态生成 / 保存 / 重跑路径需真人操作并由观察者记录。Claude Code 不能驱动登录态、也不得冒充真实用户（OPERATING-MODEL：不把自测冒充真实用户验证）。需 Owner 约到至少 1 名真实用户共享屏幕或现场操作。
2. **指标读出**：本机无法读 Production 指标（见 Gate 4 区块的阻塞记录）；需 Owner 在 Supabase SQL Editor 执行已备好的 SQL。

## Owner Dry Run Blocker — 2026-06-28

Owner 在 Production 真实使用前发现 Forge 入口存在主路径阻塞：

```text
- 输入内容后，用户以为点击「生成内容方案」就会生成，但实际只进入方向确认；第二个真正生成按钮在下方，路径不清楚。
- 空态顶部写死「正在写这次的想法」，但用户还没开始写，状态文案错误。
- 「输出语言 / 表达偏好」藏在折叠区里，没有快捷选项；用户感知为没有可选、输入没反应。
```

Codex 判定：

```text
- 这不是新功能需求，而是 V-01 前必须修的入口交互阻塞。
- 修复边界：只改 /forge 前端交互文案与控件可见性；不改 API、prompt、DB、RLS、配方、资产库、视觉渲染。
- V-01 仍不能标 Done；修复合入后重新让真实用户跑 Production 主路径。
```

### V-01-FIX-01 implementation — 2026-06-28

Implemented minimal blocker fix:

```text
- Idle shell status changed from “正在写这次的想法” to “输入一个想法开始”.
- First-step CTA changed from “生成内容方案” to “先确认方向”.
- After first-step CTA, the direction confirmation panel scrolls into view so the real generation step is visible.
- “输出语言 / 表达偏好” is now visible by default, with quick choices:
  中文 / English / Instagram carousel / LinkedIn carousel / 清空.
```

Validation:

```bash
npm run doctor
npm run lint
npm run typecheck
npm run build
FORGENOTE_BASE_URL=http://127.0.0.1:3000 npm run smoke:api
```

Result:

```text
- doctor: PASS（0 failed / 0 warnings）
- lint: PASS
- typecheck: PASS
- build: PASS after rerun with network permission for next/font Google Fonts
- smoke:api: PASS after rerun in the same permission domain as the local dev server
```

Preview Gate 3:

```text
- PR: #15
- Preview: https://forge-note-git-codex-v-01-forge-entry-fix-lstosts-projects.vercel.app
- User: dennisliu1225@gmail.com
- OAuth: Google login returned to PR #15 Preview /forge.
- Idle shell showed: 输入一个想法开始.
- Output language / expression preference was visible by default.
- Quick choice “中文” filled the field with `zh-Hans`.
- Test input: 想做一组第一次养猫预算清单的图文卡片
- First CTA: 先确认方向
- After first CTA: direction confirmation panel appeared with the second-step CTA `生成内容方案`.
- Second-step generation entered loading state and completed successfully.
- Result URL: /forge?session=c9a89de2-149a-401f-b6c8-cc689f9e7ae7
- Result included: 发布正文, 5 页逐页卡片文案, 配图方向, 发布前检查, 全部通过, and locale meta `zh-Hans`.
- Browser console: no error/warning logs captured during final result check.
```

V-01-FIX-01 result: Pass on Preview. V-01 itself is still not Done; after merge, run the real non-builder Production validation.

## Owner Dry Run Blocker 2 — 2026-06-28

Owner 在 FIX-01 合入上线后继续 dry run，发现 `/forge` 入口仍有理解阻塞：

```text
- 第一眼就是一个大输入框，空态仍能看到“新建”，用户不理解这个按钮的意义。
- 主文案“把一个模糊想法，变成可以直接开始做的内容方案”字号过大，看起来压迫、不舒服。
- 输入内容后点击“先确认方向”，页面跳到“方向待确认”，但三条方向看起来仍是内置模板，用户感知为输入没有改变结果。
- UI 整体是温暖文字风，但按钮是冷淡 SaaS 风格，不协调。
- “想更准？贴一条你发过的帖”是粘贴行为，却用了附件/上传感图标，造成心理落差。
```

Codex 判定：

```text
- 这仍是 V-01 前必须修的入口理解阻塞，不是 V-02 视觉重设计。
- 修复边界：只改 /forge 前端入口理解、方向确认反馈、按钮/图标语义；不改 API、prompt、DB、RLS、配方、资产库、视觉渲染。
- V-01 仍不能标 Done；FIX-02 合入后重新让真实用户跑 Production 主路径。
```

### V-01-FIX-02 implementation — 2026-06-28

Implemented minimal clarity fix:

```text
- 空态顶部不再显示“新建”；有草稿/结果后才显示“清空重写”。
- 首屏主文案字号从 hero 级降到工作台级。
- 方向确认区新增“本次想法”，明确复述用户刚输入的内容。
- 三条默认方向会围绕当前输入生成，例如“对「第一次养猫预算清单」有现实需求的读者 / 围绕该主题的图文清单 / 先回应该主题的具体困惑再给可执行做法”。
- 点击“先确认方向”时不再沿用上一轮默认假设，避免用户改输入后仍看到旧模板。
- “贴一条你发过的帖”图标改为粘贴语义。
- 主 CTA、生成按钮、输出偏好快捷项改为同一套暖色动作样式。
```

Validation:

```text
- Gate 2: PASS
    npm run doctor: PASS（0 failed / 0 warnings）
    npm run lint: PASS
    npm run typecheck: PASS
    npm run build: PASS after rerun with network permission for next/font Google Fonts
    FORGENOTE_BASE_URL=http://127.0.0.1:3000 npm run smoke:api: PASS after rerun with local HTTP access permission
    git diff --check: PASS
- Local browser note:
    `agent-browser` CLI is unavailable in this environment.
    Built-in Playwright is present but Chromium launch is blocked by macOS sandbox permission.
    `/forge` is also server-auth protected, so anonymous local browser verification redirects to `/login`.
    Login-state UI acceptance must be completed on PR Preview / Production with a real authenticated session.
- Preview Gate 3: 见下方「V-01-FIX-02 Preview Gate 3」段。
```

### V-01-FIX-02 Preview Gate 3 — 2026-06-28

PR 已开，CI / Vercel 绿，Preview 可达。基础设施层由 Claude Code 复核通过；登录态点击走查须 Owner 用真实 OAuth 会话完成（Claude Code 不驱动登录、不冒充真实用户）。

```text
- PR: #16  https://github.com/LSTOST/ForgeNote/pull/16
- Branch: codex/v-01-fix-02-entry-clarity @ 43b235c
- Preview: https://forge-note-git-codex-v-01-fix-02-entry-clarity-lstosts-projects.vercel.app
- CI（Doctor / Lint / Typecheck / Build）: PASS（44s）
- Vercel Deployment: PASS（ready）
- Preview 可达性（匿名）:
    /login → 200
    /forge → 307 → /login（鉴权闸正常）
- Preview smoke:api（匿名）: PASS
    非法 JSON / 缺 rawInput → 400 VALIDATION_FAILED；未登录 / 空白 → 401 AUTH_REQUIRED
- 登录态 UI 走查: PASS（2026-06-28，Owner 手动 Google 登录后由 Claude Code 经 Chrome MCP 驱动走查）
```

登录态走查结果（用户 `dennisliu1225@gmail.com`，Preview，对照 V-01-FIX-02 验收标准逐条）：

```text
环境: PR #16 Preview，登录用户 dennisliu1225@gmail.com（Owner 手动完成 Google OAuth，Claude Code 驱动其余步骤）
1. 空白首屏不出现「新建」按钮。                                     PASS（空态顶部为状态文案「✓ 输入一个想法开始」；输入后顶部变「清空重写」，非「新建」）
2. 首屏主标题不是 hero 级大字、不压过输入区。                       PASS（标题为工作台级，输入区完整可见）
3. 输入「想做一组第一次养猫预算清单的图文卡片」→ 点「先确认方向」：
   - 方向区显示「本次想法」并复述刚输入的内容。                      PASS（高亮框「本次想法：想做一组第一次养猫预算清单的图文卡片」）
   - 三条方向围绕该主题（受众 / 内容形式 / 表达角度），不像内置模板。 PASS（三条均引用该主题，非固定模板）
4. 「贴一条你发过的帖」入口图标是粘贴语义，不是上传/附件。           PASS（剪贴板图标）
5. 主 CTA / 生成按钮 / 输出偏好快捷项是同一套暖色动作样式。          PASS（「先确认方向」「生成内容方案」均暖色动作样式）
6. 点「生成内容方案」生成成功，落 /forge?session=<id>。             PASS（落 /forge?session=4b73c107-f22e-42e3-b156-624704b7b109）

证据:
- source session id: 4b73c107-f22e-42e3-b156-624704b7b109
- 结果结构: 内容定位 / 标题备选 / 发布正文 / 5 页逐页卡片文案 / 每页配图方向 / 发布前检查「全部通过」/ 5 个话题 / 保存配方区
- 浏览器 console: 无 error / exception
结论: Pass（V-01-FIX-02 入口理解修复在 Preview 登录态验证通过）。
```

## Owner Dry Run Blocker 3 — 2026-06-28

Owner 自己扮演真实用户在 Production/Preview 同码路径试用后，指出 `/forge` 页面形态仍不符合此前讨论的工作台方向：

```text
- 当前 UI 形态和工作流仍是上到下的纵向形式。
- 这不是此前讨论的左 / 中 / 右 / 底工作台布局。
```

Codex 判定：

```text
- 这不是 V-01-FIX-02 的按钮/文案级问题，而是页面信息架构仍未落到 ForgeNote 工作台形态。
- `docs/ForgeNote_修订版方向.md` §5.5 明确终态：左=账号与内容资产，中=当前内容任务与内容包，右=假设条/配方/生成控制，底=历史版本与表现回填。
- 虽然旧文档曾把完整工作台列为 M1 后逐步显形，但 Owner 的真实用户反馈说明继续验证纵向流会污染 V-01 结论。
- 修复边界：只重排现有 `/forge` 功能为最小工作台壳；不做资产库真实列表、完整历史版本、多面板拖拽、视觉渲染、自动学习；不改 API / prompt / DB / RLS。
- V-01 仍不能标 Done；FIX-03 合入后重新让真实用户跑 Production 主路径。
```

### V-01-FIX-03 implementation — 2026-06-28

Implemented minimal workbench shell:

```text
- 桌面端 `/forge` 从纵向流改为三栏 + 底栏：
    左：当前任务 / 配方库 / 账号偏好 + 账号资产 / 内容资产轻量说明。
    中：当前任务输入 + 内容方案结果。
    右：输出语言/表达偏好、方向假设/生成控制、保存配方。
    底：当前 session、复用、表现回填连续性状态。
- 移动端保持自然纵向降级，不把三栏硬挤到小屏。
- DirectionPanel 增 `compact` 模式，在右栏纵向展示三条假设。
- 生成链路、保存配方链路、session URL、output_locale、profile preference 均不改。
```

Validation:

```text
- Gate 2: PASS
    npm run doctor: PASS（0 failed / 0 warnings）
    npm run lint: PASS
    npm run typecheck: PASS
    npm run build: PASS after rerun with network permission for next/font Google Fonts
    FORGENOTE_BASE_URL=http://127.0.0.1:3000 npm run smoke:api: PASS after rerun with local HTTP access permission
- Local browser note:
    `/forge` is server-auth protected, so login-state desktop layout acceptance must be completed on PR Preview / Production with a real authenticated session.
- Preview Gate 3: PASS
    PR: #17  https://github.com/LSTOST/ForgeNote/pull/17
    Preview: https://forge-note-git-codex-v-01-fix-03-workbe-c68956-lstosts-projects.vercel.app
    User: nb19870729@gmail.com
    OAuth: Google login returned to PR #17 Preview `/forge`.
    Desktop layout: PASS after widening Chrome to desktop breakpoint.
        Left rail showed current task / recipes / account preferences plus account/content asset notes.
        Center carried current task input and later the content plan.
        Right rail carried output language / expression preference, then compact direction assumptions and generation control.
        Bottom strip showed version / reuse / performance continuity.
    Test input: first cat budget checklist carousel
    First CTA: 先确认方向
    After first CTA: compact `方向待确认` panel appeared in the right rail and showed `本次想法`.
    Second CTA: 生成内容方案
    Generation: completed successfully.
    Result URL: /forge?session=e83a0f3d-24f7-4350-b62a-af756ab07ca5
    Result included: 内容定位, 标题备选, 发布正文, 5 页逐页卡片文案, 配图方向, 发布前检查, 发布话题, 评论区引导.
    Recipe area: right rail showed `保存前先判断：这次配方值不值得留` plus `保存配方`.
    Bottom strip after result: showed current session `e83a0f3d`, reuse/save cue, and performance record cue.
    Visible app errors: none during the checked path.
```


## Owner Dry Run Blocker 4 — 2026-06-28

真实用户反馈登录入口仍有 V-01 前置摩擦：

```text
- 没有 Google 账号的话，每一次都要去邮箱点击确认。
- 这种登录方式很不人性化。
```

Codex 判定：

```text
- 这是 V-01 真实用户验证前必须修的入口问题。用户还没进入 Forge 工作台就被邮箱跳转打断，后面的可用性反馈会被污染。
- 修复边界：只改 `/login` 前端登录方式与文案；邮箱 + 密码成为非 Google 用户主路径，Magic Link 降级为备用。
- 不新增 OAuth，不做 MFA/passkey/忘记密码/账号合并，不改 `/auth/callback`、业务 API、DB、RLS、prompt。
- 如果 Supabase Production 未启用 Email password，本票 Gate 3 记录为外部配置阻塞，不能把它伪装成代码通过。
```

### V-01-FIX-04 implementation — 2026-06-28

Implemented minimal login UX fix:

```text
- `/login` 保留 Google 登录。
- 邮箱区域改为“登录 / 注册”切换。
- 已注册用户可用邮箱 + 密码登录，成功后直接进入 `/forge`。
- 新用户可创建邮箱密码账号；若 Supabase 要求邮箱确认，页面说明“首次确认一次，之后用密码直接登录”。
- Magic Link 改为虚线框备用入口，文案明确提示“不想设置密码时再用登录链接；它仍需要打开邮箱”。
- copy 资源同步更新 zh-Hans / en，继续保持 key parity。
```

Validation:

```text
- Gate 2: PASS
    npm run doctor: PASS（0 failed / 0 warnings）
    npm run lint: PASS
    npm run typecheck: PASS
    npm run build: PASS after rerun with network permission for next/font Google Fonts
    FORGENOTE_BASE_URL=http://127.0.0.1:3000 npm run smoke:api: PASS after starting local dev server
    git diff --check: PASS
- Local browser note:
    Playwright Chromium launch is blocked by macOS sandbox (`MachPortRendezvousServer` permission).
    Chrome local navigation could not be completed in this session because the desktop/browser action was blocked by the current tool usage limit.
    Do not count local visual acceptance as passed; run Preview Gate 3 on PR.
- Preview Gate 3: pending
```


## Post-FIX-01 Production Readiness Recheck — 2026-06-28 (Claude Code)

FIX-01 已合入主线并随 Vercel 自动部署到 Production；可以承接真实用户重跑。

```text
- PR #15 (V-01-FIX-01): MERGED → main HEAD cf9d631 "Fix Forge entry path for V-01"
- 入口修复文案已在 main 代码: src/lib/copy/zh-Hans.ts
    drafting 空态 = "输入一个想法开始"（不再是“正在写这次的想法”）
    第一步 CTA = "先确认方向"（不再是“生成内容方案”）
- npm run doctor : PASS（0 failed / 0 warnings）
- Production smoke:api（FORGENOTE_BASE_URL=https://forge-note-gold.vercel.app）: PASS
    非法 JSON / 缺 rawInput → 400 VALIDATION_FAILED；未登录 / 空白 → 401 AUTH_REQUIRED
- Production 路径可达性（匿名）:
    /login                     → 200
    /forge                     → 307 → /login（鉴权闸正常）
    /recipes                   → 307 → /login（鉴权闸正常）
    /recipes/<uuid>            → 307 → /login（鉴权闸正常，不泄露存在性）
```

残余风险：`/forge` 入口修复文案在登录态页面，匿名不可见，故 Claude Code 未在 Production 直接目视确认部署后的入口文案；请 Owner/测试用户在首次登录时顺带确认空态显示「输入一个想法开始」、第一步按钮为「先确认方向」。

### Owner 真实用户执行清单（V-01 唯一缺口）

请用一名**真实非构建者用户**（最好有图文卡片 / carousel 发布需求），按以下步骤跑一次 Production，全程让用户自己操作，你只观察记录：

```text
0. 准备：用户自己的真实内容想法（不要替他选题）；屏幕共享或现场。
   开场只读 Test Script 那段话，不解释 schema / 配方内部 / 指标。
1. 打开 https://forge-note-gold.vercel.app → 用 Google 或邮箱密码登录；没有 Google 账号的用户不应被迫每次去邮箱点 Magic Link。
2. 进入 /forge，确认空态显示「输入一个想法开始」。
3. 输入一个真实模糊想法。
4. 点「先确认方向」→ 确认页面滚到方向确认区，出现三条假设（受众/形式/角度）。
5. 让用户读假设条：看不看得懂？愿不愿意改？（不要指定改哪条）
6. 点「生成内容方案」→ 等生成完成。
7. 问用户：这份输出能不能直接拿去改一改就发？（不要替他判断好坏）
8. 点保存配方 → 命名 → 保存。
9. 点「查看配方」进入 /recipes/<id> 详情。
10. 在详情页换一个新输入重跑 → 落到新的 /forge?session=。
11. 记录：每步能否独立完成、卡在哪一步、原话、是否愿意保存/复用。

需要回收的证据（填进下方 Per-User Record）：
- source session id（首次生成的 /forge?session= 里的 id）
- recipe id（/recipes/<id> 的 id）
- rerun session id（重跑后 /forge?session= 的 id）
- 任一步的卡点截图或用户原话
```

观察者只在「账号/登录基础设施」卡住时帮忙；产品决策（选题、改哪条假设、内容好不好）一律交用户。

## Per-User Record

Duplicate this block for each user.

```text
User:
- Date:
- Environment:
- User type:
- Login method:
- Topic entered:

Path:
- Login completed: pass / fail / blocked
- First generation completed: pass / fail / blocked
- Assumptions understood: yes / no / unclear
- Assumption edited before generation: yes / no
- Output judged useful enough to continue: yes / no / unclear
- Recipe saved: yes / no
- Recipe detail opened: yes / no
- Recipe rerun completed: yes / no

Evidence:
- Source session id:
- Recipe id:
- Rerun session id:
- Visible blocker or confusion:
- User quote or paraphrased reaction:

Observer conclusion:
- Continue validating / fix blocker / open implementation ticket:
```

## Gate 4 — Metrics Readout

After each session batch, run:

```bash
npm run doctor
npm run metrics
```

Record the metric snapshot here:

```text
Metric readout date:
- activation:
- assumption_edit:
- recipe_save:
- recipe_rerun:
- return_session:
- performance_fill:
- Command result:
- Blocker if metrics unavailable:
```

### Metrics readout attempt — 2026-06-28 (Claude Code)

```text
Metric readout date: 2026-06-28 (not yet read)
- activation: —
- assumption_edit: —
- recipe_save: —
- recipe_rerun: —
- return_session: —
- performance_fill: —
- Command result: `npm run metrics` → SKIP exit 0（本地未设 DATABASE_URL / SUPABASE_DB_URL）。
- Blocker if metrics unavailable:
    本机直连 Production Postgres 不可行——
    (1) 本机代理 fake-ip 挡掉裸 5432（与 I-19 同一阻塞，见 docs/acceptance/I-19.md）；
    (2) 用 .env.local 的 SUPABASE_DB_PASSWORD 构造直连串被 Claude Code guardrail 拦截（[Production Reads]，未获授权）。
    既定读出路径：Owner 在 Supabase SQL Editor 手动执行下方 SQL（与 scripts/metrics.mjs 等价，只读聚合，不取任何输入全文/正文）。
```

供 Owner 在 Supabase SQL Editor 直接粘贴执行（只读，仅返回整数计数与比率）：

```sql
with m as (
  select
    (select count(distinct user_id) from public.sessions) as users_total,
    (select count(distinct user_id) from public.sessions where status='completed') as users_activated,
    (select count(*) from public.sessions where status='completed') as sessions_completed,
    (select count(*) from public.sessions s
       where s.status='completed'
         and exists (select 1 from jsonb_array_elements(s.assumptions) e where e->>'state'='edited')) as sessions_edited,
    (select count(*) from public.sessions s
       where s.status='completed'
         and exists (select 1 from public.recipes r where r.source_session_id=s.id)) as sessions_with_recipe,
    (select count(*) from public.recipes where deleted_at is null) as recipes_total,
    (select count(*) from public.recipes where deleted_at is null and usage_count>0) as recipes_rerun,
    (select count(*) from (
       select user_id from public.sessions group by user_id
       having count(distinct (created_at at time zone 'utc')::date) > 1) t) as users_returned,
    (select count(*) from public.sessions s
       where s.status='completed'
         and (s.published_at is not null or s.like_range is not null or s.favorite_range is not null
              or s.comment_range is not null or s.follower_gain_range is not null
              or s.performance_note is not null)) as sessions_perf_filled
)
select
  users_activated || '/' || users_total       as activation_rate,
  sessions_edited || '/' || sessions_completed as assumption_edit_rate,
  sessions_with_recipe || '/' || sessions_completed as recipe_save_rate,
  recipes_rerun || '/' || recipes_total        as recipe_rerun_rate,
  users_returned || '/' || users_total         as return_session_rate,
  sessions_perf_filled || '/' || sessions_completed as performance_fill_rate
from m;
```

## Completion Criteria

- At least 1 non-builder user completes or clearly fails at a named step in the Production path.
- The record includes environment, user type, topic, step outcomes, session/recipe ids when available, and the observer conclusion.
- Metrics are read out or the exact metrics blocker is recorded.
- Final status is one of:
  - Pass: at least one user completes first generation, save, and rerun without product-critical confusion.
  - Conditional Pass: the core path works but a concrete friction point must become the next ticket.
  - Fail / Blocked: login, generation, save, rerun, or metrics cannot be completed.
