# 验收：v3.17 暖纸白视觉基准

- **票 / 范围**：v3.17 视觉基准（commit `43e8132`，PR #33）——弃点阵 + 暖黄，转暖象牙底 + 暖白卡 + 衬线标题。
- **验收人**：Owner（授权 Claude Code 驱动浏览器执行路径，Owner 拍板接受令牌级证据）。
- **日期**：2026-07-05。

## 验收环境
- 本地 dev：`npm run dev -- --port 3100`（Next 16 / Turbopack；改令牌后已 `rm -rf .next` 清缓存重编）。
- 提交前门禁全绿：`doctor` 0 failed / `lint` / `typecheck` / `build` 均通过。

## 用户身份
- 登录页：匿名（无需鉴权）。
- 工作台：令牌层验证（真实登录会话属 Owner，见残余风险）。

## 用户路径与结果

### 1. 登录页 `/login` —— **Pass**（真人可见路径 + 截图）
浏览器实测（`localhost:3100/login`）：
- 令牌解析：`--background #f4f1e9`、`--card #fcfaf5`、`--border #e6dfd1`（暖纸白，与 design-tokens.md §1.1 一致）。
- `<main>` 背景 `rgb(244,241,233)` = `#F4F1E9`；`background-image: none`。
- **全页点阵扫描：0 个** `radial-gradient` 元素（旧点阵已彻底移除）。
- 标题字体：衬线栈（`ui-serif, Georgia, …`）——"内容嗓音"生效。
- Ember 登录卡身份完好（暖奶油卡 + 吉祥物），作为"暖内容卡浮于中性底"，符合 v3.17 原则。
- 控制台：0 error。
- 证据：截图（暖象牙底 + 暖白右栏 + 衬线"欢迎回来"，本会话内已留存）。

### 2. 工作台 `/workspace` —— **Pass（令牌级 verified）**
- 鉴权门实测：未登录访问 `/workspace` 正确 302 → `/login`（安全边界正常）。
- 渲染树：`/workspace` → `components/workspace/Workspace.tsx`，**不引用**旧 `forge/*` 组件（后者的硬编码色 `#f2f0e8/#18322f/#823d1d` 属旧 `/forge` M1 遗留，待 M2-04/15 退役，不在本路径）。
- `Workspace.tsx` **零硬编码 hex 色**，100% 消费令牌类：`bg-background` / `bg-card` / `border-border` / `text-foreground` / `text-muted-foreground` / `bg-primary` 等。
- 这些类全部解析为已验证的 v3.17 令牌值（`:root` 全局），中间只隔 Tailwind 工具类——故工作台渲染结果必为 v3.17，无旁路。

## 残余风险
- **工作台缺真人 authed 截图**：令牌级证据充分（组件零硬编码 + 令牌全局 v3.17），但未用真实登录会话截取 `/workspace` 实屏。理论盲区仅限"某个仅工作台使用、且带未被 grep 命中的硬编码色的子组件"——当前 `Workspace.tsx` 单文件已排除。Owner 日后任一次真实登录顺手核一眼即可闭合。
- 暗色模式（`.dark`）：登录页为硬编码浅色不体现 `.dark` 令牌；`.dark` 令牌已随 v3.17 更新（代码 review commit `43e8132` 已核 hex 逐一对齐），但未在真人路径下截取暗色工作台。
- 橙 primary 白字对比 3.37:1（大字级 AA）：既有爱马仕橙固有属性，非 v3.17 引入；小字场景建议组件层用深橙 `#B4490F`（Codex P2-1，未阻断）。

## 判定
登录页 **Pass**（真人路径 + 证据）；工作台 **Pass（令牌级）**，真人 authed 截图列为可闭合残余风险。v3.17 视觉基准验收通过。
