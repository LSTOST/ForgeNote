# I-15 Acceptance — v5 选择性折叠文案 / 产品表述收敛

> 票：把产品表述从强「小红书单平台」逐步折叠为「国际 + 图文卡片 / carousel + 多语言准备」的通用表达，additive，不推翻现有 M1 v4 小红书实现。
> **状态：Done。** 自动验证、匿名权限边界无回归、真实 Chrome 登录态首屏/示例/主流程/配方页文案均已通过。

## 范围澄清

本轮 I-15 按执行指令聚焦**产品文案 / UI 表述 / 文档表述的 v5 收敛**（in-place 文案，不改行为）：

- I-15 是产品文案 / UI 表述 / 文档表述收敛。
- I-15 **不是** schema migration、不是 prompt 国际化、不是多语言生成功能、不是 Profile 偏好。
- 原 DECISIONS D-07(a) 设想的「i18n 文案抽资源文件 + en/zh-Hans 脚手架」属更重的工程化外化，**未在本轮做**；如仍需要，作为后续 i18n 工程化票另立（见 TICKETS 注）。

## 范围内（本轮实际改动）

- **产品定位文案**：从「只服务小红书」收敛为「图文卡片 / carousel 内容工作台」；保留小红书为适用场景之一，不删除 v4 能力；不夸大为全平台自动发布工具；不暗示已支持多语言生成（仅“为多语言准备”的战略表述）。
- **`src/lib/constants.ts`**：
  - `PRODUCT_NAME` = `ForgeNote 图文卡片内容工作台`
  - `SLOGAN` = `把模糊想法，锻造成可发布的图文卡片内容包`
  - 新增 `POSITIONING`：点明 carousel + 小红书 / Instagram carousel / LinkedIn carousel 场景，并诚实声明「M1 仍以图文卡片和发布文案为主，不做自动发布」
  - `IDEA_PLACEHOLDER` 去小红书化（改为「图文卡片」）
  - `EXAMPLE_IDEAS`：保留 1 个小红书场景 + 补 Instagram carousel 场景 + 1 个通用图文卡片场景
- **`src/app/layout.tsx`**：metadata `title` / `description` 同步收敛。
- **`src/app/forge/page.tsx`**：header 增加 `POSITIONING` 副标题（一行定位，不做营销页、不大改版式）。
- **`src/components/forge/OutcomePanel.tsx`**：正文区 Section 标题与“复制全文”拼接标题「小红书正文」→「发布正文」（纯 UI / 剪贴板文案，无行为变化）。
- login / forge / TopNav header 通过 `PRODUCT_NAME` / `SLOGAN` 常量自动收敛；`/recipes`、`/recipes/[id]` 既有说明文案已中性，无冲突。
- **文档**：`docs/UIUX-M1.md` §4.1 / §5.1 / §5.3 / §5.4 文案方向轻量同步；`docs/PROJECT-STATUS.md`、`docs/TICKETS.md` 状态同步；新增本文件。

## 明确不做（守住边界）

- 不改 Supabase migration / RLS / 字段；不做 `output_locale`、不做 locale selector、不做多语言实际生成 / 自动翻译。
- 不改模型 prompt / OpenRouter 调用 / `src/lib/ai/generate.ts` 生成策略。
- 不做 `carousel_package` intent、不改 `content_package`、不改 D-01、不改 DATA-SCHEMA 枚举、不改 API-CONTRACT 核心字段。
- 不改保存 / 删除 / rerun 业务逻辑；不做 Profile / 偏好 / F-16；不引入新依赖。
- 按边界**保留不改**的“小红书”出现位置：`xiaohongshu_note` intent 的展示标签「小红书笔记」（平台特定 intent，准确）、`generate.ts` 系统 prompt 与验收 message（prompt 国际化非 I-15 范围）、`ForgeWorkbench` 默认假设种子 `platform=小红书`（可编辑默认值，改动会进入生成请求=行为变化）、`mock-generator.ts`（非线上生成路径）。

## 自动验证

| 命令 | 结果 |
|---|---|
| `npm run lint` | 通过 |
| `npm run typecheck` | 通过 |
| `npm run build` | 通过（路由表不变） |
| `npm run doctor` | 通过（0 failed / 0 warnings） |
| `npm run smoke:api` | 通过（`/api/forge` 匿名边界无回归） |

## 登录态 Chrome 验收（通过）

- `/forge` 首屏：标题「ForgeNote 图文卡片内容工作台」、slogan「把模糊想法，锻造成可发布的图文卡片内容包」、定位副标题含 carousel 与小红书 / Instagram carousel / LinkedIn carousel 场景及「不做自动发布」声明 —— 第一感知不再是“只做小红书”。
- 示例想法：3 个中含 1 个小红书 + 1 个「Instagram carousel」+ 1 个通用图文卡片场景。
- 生成主流程：输入框、字数、「开始锻造」按钮在空输入 disabled / 有输入 enabled，空态文案正常；OutcomePanel 正文区标题显示「发布正文」。
- `/recipes` 列表：定位文案中性无冲突；「小红书笔记」仅作为类型筛选项出现（准确）。
- `/recipes/[id]` 详情：字段与「换输入重跑」面板文案无冲突；既有 recipe 数据不变（`usage_count` 保持 I-10 验收后的 1）。
- 行为回归：保存 / 删除 / rerun 逻辑未改动，仅文案调整，未见回归。

> 备注：`/login` 在已登录态下重定向 `/forge`，其标题/slogan 与 forge 共用 `PRODUCT_NAME` / `SLOGAN` 常量，已随收敛；未登出验证未登录态登录页（避免影响用户会话），登录页文案以常量来源核对。

## 结论

- 当前状态：**Done**。
- 完成依据：自动命令全过；匿名边界无回归；真实 Chrome 登录态下首屏定位、示例、生成主流程、配方列表 / 详情文案均已收敛为图文卡片 / carousel 方向，且保存 / 删除 / rerun 行为与 `content_package` 等内部契约不变。
