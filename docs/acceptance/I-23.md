# I-23 Acceptance — Saved Recipe Reuse Proof

## 结论

- Status: Review
- Date: 2026-06-25
- Scope implemented: save success now exposes the saved recipe detail path, so the user can continue from `/forge?session=` to `/recipes/<id>` and rerun the recipe.
- Branch: `codex/i-23-recipe-reuse-proof`
- PR: Pending

## Gate 1 — Product Intent

- User: logged-in solo creator who has generated an I-22 structured content plan.
- Task: save the useful result as a recipe, open that recipe, rerun it with a new input, and confirm the new output keeps the same structured content-plan shape.
- Changed path: `/forge?session=<completed>` save recipe success state.
- Loss if not done: "Save recipe" remains a dead-end confirmation. It does not prove the recipe can be reused from the detail/rerun path.
- Out of scope: asset library, visual rendering, image generation, visual recipe system, automatic learning, recipe scoring, content calendar, multi-account, DB schema/RLS changes, and `content_package` renaming.

## Implementation

- `src/components/forge/RecipePanel.tsx`
  - Stores the `recipeId` returned by `POST /api/recipes`.
  - Treats a missing `recipeId` in an otherwise successful response as a save failure.
  - Shows a `查看配方` / `View recipe` link to `/recipes/<recipeId>` after save success.
  - Adds a short reuse hint after save success so the next user action is recipe detail plus rerun, not stopping at "saved".
- `src/lib/copy/{zh-Hans,en}.ts`
  - Adds copy for the post-save recipe detail link and reuse hint.

No API change, database migration, RLS change, prompt change, or route change.

## Gate 2 — Automatic Validation

```bash
npm run doctor
npm run lint
npm run typecheck
npm run build
FORGENOTE_BASE_URL=http://127.0.0.1:3000 npm run smoke:api
```

Result: pass.

Notes:
- First `npm run build` inside sandbox failed because `next/font` could not fetch Geist from Google Fonts. Re-running with network permission passed.
- First `smoke:api` from the non-escalated sandbox could not reach the dev server started in an escalated permission domain. Direct curl in the same permission domain confirmed `/login` 200 and anonymous `/api/forge` → `AUTH_REQUIRED`; re-running `smoke:api` in that same domain passed.

## Gate 3 — User Path Acceptance

Required path:

1. Logged-in user opens an I-22 completed session at `/forge?session=<id>`.
2. User saves the recipe.
3. Save success shows a `/recipes/<recipeId>` link.
4. User opens the recipe detail page.
5. Recipe detail shows the source session, core fields, and rerun form.
6. User enters a new topic and reruns.
7. App lands on `/forge?session=<newSessionId>`.
8. New result still includes:
   - `发布正文`
   - 5-8 page-by-page card drafts
   - visual direction
   - pre-publish checklist
9. Original recipe is not overwritten; `usage_count` increases after successful rerun.

Current Preview check:
- PR: PR #12 `https://github.com/LSTOST/ForgeNote/pull/12`
- Preview: `https://forge-note-git-codex-i-23-recipe-reuse-proof-lstosts-projects.vercel.app`
- CI/Vercel: green; merge state clean.
- Browser opened `/forge` and was redirected to `/login`, so this Preview domain does not currently have an authenticated session.
- Clicking Google login from Chrome automation was blocked by browser security policy because the OAuth flow touches `forge-note-gold.vercel.app`, a domain the user has explicitly said should not be used by the agent.
- Required next action: Owner manually logs into the exact PR #12 Preview URL, then Codex resumes Gate 3 from `/forge`.

Current result: Blocked pending Owner login on the exact PR #12 Preview domain.

## Residual Risk

- Old recipes saved before I-22 may only contain legacy `cardPrompts.prompt`; I-23 acceptance must use a recipe saved from an I-22 structured session. Old data compatibility remains "do not white-screen", not "retroactively upgrade content quality".
