# I-08 Acceptance — 保存配方最小闭环

> 票：生成完成后，用户在 RecipePanel 点「保存配方」、命名、写入 `recipes` 表，并得到成功/失败反馈。
> **状态：Done。** 自动验证、匿名 API 边界、登录态核心保存闭环与 Supabase 字段核对均已通过。

## 范围内

- 新增 `POST /api/recipes`（必须登录；`{ sessionId, name }`）。
- `RecipePanel` 成功态启用「保存配方」+ 轻量命名 UI + 保存中/已保存/失败反馈。
- `ForgeWorkbench` 把 `sessionId` 透传给 `RecipePanel`。

## 范围外（硬限制）

- 不做 `/recipes` 列表页、配方详情、换输入重跑。
- 不做偏好记忆 / `profile_preferences`、不做 F-16。
- 不改模型 prompt、不改生成逻辑。

## 实现要点

- 错误码与 HTTP：`AUTH_REQUIRED` 401 / `VALIDATION_FAILED` 400 / `SESSION_NOT_FOUND` 404 / `DATABASE_ERROR` 500（对齐 API-CONTRACT §3 / §5.5）。
- 校验顺序：解析 JSON → body schema → **鉴权** → name trim 非空 → 读 session → 有无 `recipe_snapshot` → 插入。鉴权早于业务校验（与 `/api/forge` 一致）。
- session 读取经 RLS（`auth.uid() = user_id`）：他人/不存在的 `sessionId` 都查不到 → `SESSION_NOT_FOUND`，不泄露存在性。
- `recipes` 写入映射：`user_id=user.id`、`name=trimmed`、`intent_type=session.intent_type`、`schema={intentType,version:1}`、`fields=recipe_snapshot`、`acceptance/variables/negative_rules` 从快照拆出、`source_session_id=session.id`。
- 同名允许：每次保存都是独立 INSERT，不 upsert、不覆盖。
- 前端：成功态显示「保存配方」；命名 UI 默认值用 `recipe.name`；名称为空禁用保存；保存中 loading；成功显示「已保存到配方库」；失败显示 inline error 且不清空已生成结果。切换 session 时以 `key={sessionId}` 重挂面板重置保存态。

## 自动验证

| 命令 | 结果 |
|---|---|
| `npm run lint` | 通过 |
| `npm run typecheck` | 通过 |
| `npm run build` | 通过（新增动态路由 `ƒ /api/recipes`，其余路由不变） |
| `npm run doctor` | 通过（0 failed / 0 warnings） |
| `npm run smoke:api` | 通过（`/api/forge` 匿名边界无回归） |

## API 验收（匿名，dev server 实测）

| 场景 | 请求 | 预期 | 实测 |
|---|---|---|---|
| 未登录保存 | 合法 UUID + name | 401 `AUTH_REQUIRED` | ✅ 401 `AUTH_REQUIRED` |
| 未登录 + 空 name | 合法 UUID + `"   "` | 401（鉴权早于 name 校验） | ✅ 401 `AUTH_REQUIRED` |
| 非法 JSON | `{` | 400 `VALIDATION_FAILED` | ✅ 400 `VALIDATION_FAILED` |
| 缺字段 | `{"name":"x"}` | 400 `VALIDATION_FAILED` | ✅ 400 `VALIDATION_FAILED` |

> 说明：测试 UUID 需符合 RFC 变体位，否则被 zod 在鉴权前判 `VALIDATION_FAILED`（已用 `uuidgen` 生成合法 UUID 复测通过）。

## 登录态验收

- [x] 登录后保存当前生成 session → `recipes` 新增记录，`source_session_id` / `user_id` / `fields` 正确写入。
- [x] 同名保存两次 → 两条独立 recipes，**不覆盖**。
- [x] UI：保存成功显示「已保存到配方库」。
- [x] UI：空名称时「保存」按钮 disabled，不能提交。
- [x] Supabase 字段核对：同一 session 下 `recipe_count=2`，且 `user_id` / `source_session_id` / `intent_type` / `fields` / `acceptance` / `variables` / `negative_rules` 均匹配 session 快照。

> 残余风险说明：认证态负例未通过 HTTP 直接执行，原因是浏览器自动化环境不能读取 cookie、也不提供可用的 `fetch` / `XMLHttpRequest` 来代表当前登录页发任意 API 请求。上述负例已按 `src/app/api/recipes/route.ts` 的确定性校验顺序做代码路径核对：鉴权后依次执行 name trim、RLS session 查询、`recipe_snapshot` 校验。

未直接 HTTP 执行的补充负例：

- 无 `recipe_snapshot` 的 draft session → 400 `VALIDATION_FAILED`。
- 不存在 / 非本人的 `sessionId` → 404 `SESSION_NOT_FOUND`。
- 空 name → 400 `VALIDATION_FAILED`。

手测步骤（具备登录态时）：`npm run dev` → 登录 → `/forge` 真实生成一次 → 保存配方 → 在 Supabase `recipes` 表确认 `source_session_id` / `user_id` / `fields` 写入正确，再保存同名一次确认两条独立。

## 结论

- 当前状态：**Done**。
- 完成依据：核心用户路径“登录态真实生成 → 保存配方 → recipes 落库 → 同名二次保存不覆盖 → 字段映射正确”已在真实 Chrome 登录态 + Supabase SQL Editor 下验证。
- 残余风险：认证态 API 负例未直接 HTTP 执行，但 route 代码路径清楚、匿名边界已通过，且 UI 空名称禁用已验证；不阻断 I-08 Done。

## 验收记录

### 2026-06-20 — 登录态自动验收尝试：Blocked

- 自动验证：`npm run lint` / `typecheck` / `build` / `doctor` / `smoke:api` 全部通过。
- 匿名 API 边界（dev server 实测）：合法 body → 401 `AUTH_REQUIRED`；非法 JSON / 缺字段 → 400 `VALIDATION_FAILED`。通过。
- 登录态保存落库：**未复验**。本次自动环境无法获得真实 Supabase 登录态——
  - 缺 `SUPABASE_SERVICE_ROLE_KEY`（无法建/确认测试用户、无法 mint session）；
  - 匿名 `signUp` 受项目邮箱确认策略限制，不返回即时 session；`signInWithPassword` 因未确认失败；
  - Magic Link / OAuth 需读邮件或交互式浏览器登录，自动环境不可行。
  - 验收尝试在拿不到 session 时即退出，**未创建任何 recipes / sessions 行**；一次性脚本未提交、已删除（运行时只输出状态码/字段布尔，不打印 cookie/token/env）。
- 代码结论：未发现 I-08 代码 bug，无改动。
- **阻塞解除条件（满足其一即可跑完上方 6 项清单）**：
  1. 提供 `SUPABASE_SERVICE_ROLE_KEY`（建+确认一次性测试用户，跑完即删数据）；或
  2. 提供一个**已确认**测试用户的 email + password；或
  3. 在 Supabase 控制台临时关闭邮箱确认；或
  4. 人工浏览器登录 `/forge` 后按本文件清单逐项手测。

### 2026-06-20 — Chrome 登录态验收：通过

- 入口：使用用户 Chrome 现有 Supabase 登录态访问 `http://localhost:3000/forge`，页面显示已登录用户。
- 真实生成：输入“想做一组小红书卡片，主题是第一次独居备用金清单”，`POST /api/forge` 返回 200，页面出现 completed session。
- 保存配方：RecipePanel 打开命名 UI，默认名称为模型返回的 `recipe.name`；点击保存后 UI 显示「已保存到配方库」，服务端日志记录 `POST /api/recipes 200`。
- 同名不覆盖：再次保存同名配方，UI 再次成功，服务端日志记录第二次 `POST /api/recipes 200`。
- Supabase SQL Editor 核对：按该 session 聚合查询，结果为 `recipe_count=2`，且 `user_id_matches` / `source_session_matches` / `intent_type_matches` / `fields_match` / `acceptance_match` / `variables_match` / `negative_rules_match` 全部为 `true`。
- 空名称 UI：清空配方名称后，「保存」按钮 disabled，无法提交。
- 临时 draft 负例准备：曾通过 SQL Editor 创建一条临时 draft session 用于负例准备；因浏览器自动化无法代表当前页面发任意 XHR/fetch，未直接命中 API；临时 draft session 已删除。
