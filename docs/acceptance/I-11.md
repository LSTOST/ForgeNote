# I-11 Acceptance — 偏好页 `/profile`：edited assumptions 写入并下次带出

> 票：受保护 `/profile` 管理当前用户的偏好假设（profile_preferences）；`/forge` 下次生成把偏好作为 `source="profile"` 假设带出；可从 Forge 把已编辑的假设「记住为偏好」。
> **状态：Done。** 代码、自动验证、匿名边界、真实 Chrome 登录态 CRUD / 带出 / 生成落 session / 「记住为偏好」/ 删除后不再带出均已通过。

## 范围内

- 受保护 `/profile`（Server Component 鉴权，未登录 → `/login`）。
- 偏好 CRUD：
  - `GET /api/profile/preferences`（列出当前用户偏好）
  - `POST /api/profile/preferences`（创建/更新，按唯一键 `(user_id, intent_type, dimension_key)` upsert）
  - `PUT /api/profile/preferences/:id`（改 value）
  - `DELETE /api/profile/preferences/:id`（删除单条）
- `/profile` UI：新增表单 / 行内编辑值 / 删除防误触 / 空态 / 刷新态 / 保存中 / 成功反馈 / 错误态。
- edited assumptions 写入：`/forge` AssumptionPanel 每条假设加「记住为偏好」（★）按钮 → upsert profile_preferences。
- 下次带出：`/forge` Server Component 按 RLS 读取 `content_package` 偏好 → 映射为 `source="profile"` 假设 → 作为 ForgeWorkbench 初始假设种子；生成时随假设提交并落入 `session.assumptions`；AssumptionPanel 显示「来自偏好」。

## 范围外（硬限制）

- 不做自动学习 / 不从历史 session 自动总结偏好。
- 不做 F-16 / I-12 表现回填、不做 I-17 i18n 资源外化、不做 UI 多语言切换。
- 不改 `content_package` / `output_locale` / 模型 prompt / RLS。
- 不引入新依赖。

## 数据层

- **无 migration**：`profile_preferences` 表与 RLS（`auth.uid() = user_id`，唯一键 `(user_id, intent_type, dimension_key)`）在 `0001_init.sql` 已存在。
- 偏好以「假设维度」存储：`intent_type` + `dimension_key` + `dimension_label` + `value` + `source`（M1 固定 `manual`）。

## 偏好注入设计（关键决策）

- 注入点选在 **`/forge` Server Component**（DAL 模式，与现有 session 预载一致）：读取偏好 → 作为 `source="profile"` 初始假设传入 ForgeWorkbench。
- **不在 `/api/forge` 内重复注入**：否则用户在 Forge 内 dismiss 掉某条 profile 假设后，服务端会因「无法区分『被删除』与『客户端未传』」而把它重新加回——这是真实 bug。改由 Server Component 注入种子 + 客户端按既有 dismiss/edit 流程提交，dismiss 自然生效。
- **back-compat**：无偏好 → 种子为空 → `/forge` 与 `/api/forge` 行为与之前完全一致（旧请求兼容）。

## 鉴权 / 错误码（对齐现有风格）

- 所有偏好 API 必须登录：未登录 → `AUTH_REQUIRED`（401）。
- 参数不合法 / 非法 JSON / 三字段 trim 后为空 / 非 canonical intent → `VALIDATION_FAILED`（400）。
- 单条 PUT/DELETE 的他人 / 不存在 / 非法 id → `PREFERENCE_NOT_FOUND`（404，不泄露存在性）。
- DB 异常 → `DATABASE_ERROR`（500）。鉴权顺序：解析 body →（写）鉴权 → 校验 → RLS 读写。

## 自动验证

| 命令 | 结果 |
|---|---|
| `npm run lint` | 通过 |
| `npm run typecheck` | 通过 |
| `npm run build` | 通过（新增 `ƒ /profile`、`ƒ /api/profile/preferences`、`ƒ /api/profile/preferences/[id]`，其余路由不变） |
| `npm run doctor` | 通过（0 failed / 0 warnings） |
| `npm run smoke:api` | 通过（`/api/forge` 匿名边界无回归） |

## 匿名 API 边界（dev server 实测）

| 场景 | 预期 | 实测 |
|---|---|---|
| `GET /api/profile/preferences`（匿名） | 401 `AUTH_REQUIRED` | ✅ |
| `POST` 合法 body（匿名） | 401 `AUTH_REQUIRED` | ✅ |
| `POST` 非法 JSON | 400 `VALIDATION_FAILED` | ✅ |
| `POST` 非 canonical intent | 400 `VALIDATION_FAILED` | ✅ |
| `PUT /:id`（匿名） | 401 `AUTH_REQUIRED` | ✅ |
| `DELETE /:id`（匿名） | 401 `AUTH_REQUIRED` | ✅ |
| 未登录访问 `/profile` | 307 → `/login` | ✅ |

## 真实 Chrome 登录态验收

账号：`huggingme@duck.com`。本轮先遇到 Chrome 扩展断连；用户打开新 Chrome 窗口后 Codex Chrome 通道恢复，完成以下复测：

1. ✅ `/profile` 可访问；无偏好时显示空态（`共 0 条偏好` / `还没有偏好`）。
2. ✅ 新增偏好：内容类型=内容包、维度名=语气、key=`tone`、值=`成熟克制，不焦虑，不夸张` → 列表出现该条。
3. ✅ 刷新后偏好仍在。
4. ✅ 行内编辑值为 `成熟克制，不焦虑，带一点行动感` 并保存 → 显示「已更新偏好」，刷新后仍在。
5. ✅ `/forge` 自动带出该偏好：AssumptionPanel 显示 `语气：成熟克制，不焦虑，带一点行动感`，并显示「来自偏好」。
6. ✅ 生成一次：新 session `89ecf330-feea-4f77-9e94-d4375085b11e` 成功生成；结果配方「语气风格」使用 profile 值。
7. ✅ `/forge?session=89ecf330-feea-4f77-9e94-d4375085b11e` 回填后仍显示该假设与「来自偏好」标签，等价证明 `session.assumptions` 持久化 profile 来源。
8. ✅ 在 `/forge` 行内编辑该假设为 `成熟克制，适合收藏，行动步骤明确`，点击「记住为偏好」→ 显示「已记住」；回 `/profile` 确认偏好被 upsert 为新值。
9. ✅ 删除防误触：点击「删除」先出现「确认删除？」；点击「取消」不删除；再次删除并「确认删除」后列表回到空态。
10. ✅ 删除后刷新 `/profile` 仍为空；再次进入 `/forge` 不再带出该偏好，也不再显示「来自偏好」。

> 注：偏好读写均经 RLS（`auth.uid() = user_id`）+ route 内 `user_id` 条件；单条 PUT/DELETE 对他人/不存在 id 返回 `PREFERENCE_NOT_FOUND`，已由代码路径保证。

## 结论

- 当前状态：**Done**。
- 残余风险：无新的 I-11 阻塞项。直接打开 DB/JSON 读取 `session.assumptions` 未做；已通过 `/forge?session=` 服务端预载回填验证用户可见链路。
- 下一张唯一任务：I-12 — F-16 表现回填 lite。
