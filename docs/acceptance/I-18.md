# I-18 Acceptance — 补齐 UI copy 资源覆盖

> 票：把 M1 活跃页面仍硬编码的 UI 文案收敛到 `src/lib/copy/{zh-Hans,en}.ts`，默认 zh-Hans 行为不变。承接 I-17 scaffold。
> **状态：Done。** 自动验证全过；浏览器 smoke 无 undefined / raw key / [object Object]；默认 UI 仍为中文、行为不变。

## 范围内（已交付）

- 扩展 `zh-Hans` / `en` copy key（`en: Copy` 编译期同构，typecheck 保证 key 不漂移）。新增分组：`common` / `intentTypes` / `login` / `idea` / `forge` / `outcome` / `recipePanel` / `assumptions` / `performance` / `recipes` / `recipeDetail` / `profile`（在 I-17 基础上大幅补齐）。
- 含 `{n}` / `{max}` / `{label}` / `{date}` / `{count}` 占位符的值由调用处 `.replace(...)` 注入（保持 copy 值为纯 string，`en: Copy` 类型成立）。
- 接线（消费 `copy.*`）的活跃 UI：
  - `src/components/auth/LoginForm.tsx`
  - `src/components/forge/IdeaInput.tsx` / `ExampleIdeas.tsx` / `ForgeWorkbench.tsx`（输出语言标签 + 生成失败/网络错误回退）
  - `src/components/forge/OutcomePanel.tsx`（含复制全文/卡片导出标题、section 标题、空/错误/登录态）
  - `src/components/forge/RecipePanel.tsx`（含配方摘要导出 label、字段 label、验收块）
  - `src/components/forge/AssumptionPanel.tsx`（含 aria / title / 标签）
  - `src/components/forge/PerformancePanel.tsx`
  - `src/components/recipes/RecipesLibrary.tsx`（含 intent 枚举标签、空态、删除态）
  - `src/components/recipes/RecipeRerun.tsx`
  - `src/app/recipes/[id]/page.tsx`（含 NotFound 态、字段 label、intent 标签）
  - `src/components/profile/ProfilePreferences.tsx`（含新增表单、行内编辑、删除态、空态）
- intent 类型标签集中到 `copy.intentTypes`，RecipesLibrary / recipes[id] / ProfilePreferences 三处复用，去重。

## 范围外（未做）

- 不做运行时语言切换 / locale route / middleware / cookie / profile 持久化。
- 不联动 `output_locale`。
- 不改 prompt / DB / RLS / API。
- 不做视觉 redesign。
- `constants.ts` 产品级文案（`PRODUCT_NAME` / `SLOGAN` / `POSITIONING` / `IDEA_PLACEHOLDER` / `EXAMPLE_IDEAS`）保留为单一来源，未复制进 copy（避免双源漂移）。

## 自动验证

| 命令 | 结果 |
|---|---|
| `npm run lint` | 通过 |
| `npm run typecheck` | 通过（`en: Copy` 同构保证 key 不漂移） |
| `npm run build` | 通过（路由表不变） |
| `npm run doctor` | 通过（0 failed / 0 warnings） |
| `npm run smoke:api` | 通过 |

## 浏览器 smoke（本地 dev，登录态）

| 页面 | 结果 |
|---|---|
| `/login` | 200；无 undefined / raw key / [object Object] / 未替换占位符 |
| `/forge` | 渲染「开始锻造」「试试这些示例」「输出语言 / 表达偏好」；无泄漏 |
| `/recipes` | 200；无泄漏 |
| `/recipes/[id]` | 渲染「返回配方库」「配方字段」「换输入重跑」；无泄漏 |
| `/profile` | 200；无泄漏 |

## 剩余硬编码中文清单（rg 复扫）

- **允许保留**：各文件注释（`//`、`/* */`、UIUX/票号引用）。
- **允许保留**：`src/components/forge/ForgeWorkbench.tsx` 的 `DEFAULT_ASSUMPTIONS`（平台/内容形式/受众/语气 + 值如「小红书」「第一次独居的人」）——属**假设种子数据/内容**（提交进生成请求），非 UI chrome，性质同 mock/generated 内容；其 label 与值耦合，不宜外化为 UI copy。
- **允许保留**：`src/lib/ai/mock-generator.ts`、`src/lib/ai/generate.ts`（prompt / 生成内容，范围外）。
- **主要 UI chrome**：已无散落硬编码（活跃组件均消费 `copy.*`）。

## 结论

- 当前状态：**Done**。默认中文 UI 与之前逐字一致、行为不变；英文为 scaffold（未润色）。
- 残余风险：英文 scaffold 未做产品级润色（符合范围）；运行时语言切换为后续票。
