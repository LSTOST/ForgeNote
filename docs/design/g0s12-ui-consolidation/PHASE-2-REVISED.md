# PHASE-2-REVISED 指令 · 单工作台外壳返工（喂给 Cursor 的完整提示词）

你是 ForgeNote 仓库的执行工程师。阶段 2 灰稿原型已产出，但 Owner 点完后拍板改架构：**从「多页 + 各自导航」改为「单工作台外壳 + 壳内视图切换」**。本任务是按新架构**返工阶段 2 的线框与原型**。**仍然不碰 `src/**`。**

## 唯一依据（先读，其他别读）

1. `docs/design/g0s12-ui-consolidation/UX-BASE.md` **§2.3「单工作台外壳」**——这是当前生效 IA，覆盖 §2.1 导航列和 §2.2 的 IA-1。逐条照做。
2. `docs/design/WORKSPACE-REDESIGN-G0S10.md`（工作台三区模型，写作视图沿用）
3. `docs/DESIGN.md` §3/§4/§9（间距、布局、反模式）

## 核心变化（相对你上一版原型）

- 不再有 8 个独立页面各带导航。改为**一个外壳**：左栏（导航+最近内容+账号菜单）+ 主区（随左栏切换显示：写作/选题/账号分析/周看板）。
- **first-run 没了**，并入写作视图空状态：主区无进行中内容时显示「今天想写哪条内容」输入。
- **profile 没了**（隐藏）：任何线框、原型、导航都不得出现「个人偏好」入口。
- **旧 TopNav 没了**：不画任何 dashboard 顶栏。
- 路由保留（实现层是 Next.js 共享布局），但线框/原型只需表达「左栏切换主区视图」的体验；URL 保留这件事在阶段 3 实装，线框里用注释标明每个视图对应的路由即可。

## 产出 A · 修订 `docs/design/g0s12-ui-consolidation/WIREFRAMES.md`

重写为**外壳 + 4 个主区视图**结构（不再是 8 个独立页）：

1. **外壳线框**：左栏（BrandMark / 新写一条 / 最近内容 / 本周可写选题 / 辅助组[周看板·账号分析] / 页脚账号菜单）+ 主区容器。标注左栏在视图切换时不重载。
2. **写作视图**（默认）：含空状态（「今天想写哪条内容」输入，= 原 first-run）与工作流态（Stage 0→A→B→产出区，沿用 G0S-10）。
3. **选题视图**（/radar）：选题卡列表 → 点击进写作视图预填。
4. **账号分析视图**（/account）：账号资料输入 + 可读判断展示。
5. **周看板视图**（/gate0）：使用统计 + 表现回填表单。
6. **登录**（/login）：外壳外的唯一独立页，保留。
每个视图仍给状态表（空/加载/错误/成功）+ 交互说明 + 与现状差异。文案遵守 `docs/UX-WRITING.md` §13。

## 产出 B · 修订 `docs/design/g0s12-ui-consolidation/prototype.html`

- 单文件、零依赖、纯灰阶（一个占位强调色）。
- **左栏常驻**，点击左栏项切换主区视图（写作/选题/账号分析/周看板），左栏本身不变——这是本次要验证的核心体验。
- 走通 5 条任务流（UX-BASE §3）：新写一条、从选题开工、重开继续、发布后回填、账号维护——全部在外壳内切换完成，不跳独立页。
- 不出现：first-run 独立页、profile 入口、旧 TopNav。

## 边界（违反即失败）

- 不碰 `src/**`、不改 tokens、不做高保真视觉。
- 不新增 UX-BASE §2.3 之外的视图或功能。
- 严格执行隐藏 profile：grep 你产出的文件，确认无「个人偏好 / profile」入口。

## 收尾

- roadmap G0S-12 note 追加「阶段 2 返工：单工作台外壳 + 4 视图线框 + 灰稿原型」。
- 产出后**停下**，输出 prototype.html 路径，请 Owner 再点一遍。布局冻结后才进阶段 3。
