# 归档说明

本目录存放已停止维护的历史进度文档（2026-07-05 归档）：

- `PROJECT-STATUS.md` / `TICKETS.md` / `PROJECT-GANTT.md` — M1 时代的进度记录，
  内容停留在 M1/DSN 阶段，已与实际进度（M2）脱节。**只作历史参考，不再更新。**

项目进度唯一事实源现为 **`docs/roadmap/roadmap.json`**（方向 → 里程碑 → 票），
可视化面板用 `npm run progress` 生成。更新规则见根目录 `AGENTS.md`。

历史文档中指向 `docs/PROJECT-STATUS.md` 等旧路径的链接（acceptance、design 目录内）
属于历史记录的一部分，不回改。
